# Post 1 (local) dependencies.
# Pinned loosely to minor versions; run `pip install -U` and re-pin per post.
# Later posts add: bedrock-agentcore (Runtime/Memory SDK) and dev deps.
strands-agents>=0.1.0
boto3>=1.35.0

# Install the notes_agent package itself (src/ layout). Without this, the
# package isn't importable and `python -m notes_agent.main` fails with
# ModuleNotFoundError. Requires running from the repo root.
-e .
