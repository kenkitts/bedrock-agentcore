# Post 3 (Memory) dependencies.
# Pinned loosely to minor versions; run `pip install -U` and re-pin per post.
#
# Note: no `-e .` here. The notes_agent package lives at the repo root next to
# app.py, so it imports by co-location (the entrypoint's directory is on the
# import path) both locally and inside the deployed Runtime container — no
# install step required.
strands-agents>=0.1.0
boto3>=1.35.0

# AgentCore SDK. Post 2 used this for BedrockAgentCoreApp / @app.entrypoint (the
# HTTP server the Runtime hosts). Post 3 adds the [strands-agents] extra, which
# pulls in the AgentCore Memory <-> Strands session-manager integration
# (bedrock_agentcore.memory.integrations.strands) used by notes_agent/memory.py.
bedrock-agentcore[strands-agents]>=0.1.0
