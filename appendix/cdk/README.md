# Appendix: Productionizing with CDK

The series uses the AgentCore CLI (`@aws/agentcore`) for provisioning to keep
each post focused on the AgentCore concept rather than infrastructure-as-code.

This appendix — written once the full agent exists (after Post 7) — shows how to
reproduce the same resources (Runtime, Memory, Gateway, Identity, IAM) as AWS CDK
constructs for teams that want reproducible IaC.

Status: placeholder. To be authored after the core series is complete.
