"""Deterministic unit tests for the notes agent.

Per the series plan (B-lite testing), real tests begin in Post 4 (Gateway),
where the notes/bookmarks tool functions become deterministic, pure code worth
asserting. Agent *behavior* evaluation is taught later, in Post 7
(Observability), not here.

This placeholder keeps the test target green for Posts 1-3.
"""


def test_placeholder_until_post_4():
    assert True
